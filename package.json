{
  "name": "shape-onboarding",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone prototype for Shape Rotator preference onboarding with hidden shape reveal and PR-ready markdown export.",
  "scripts": {
    "build": "node scripts/build-public.js",
    "start": "node server.js",
    "verify": "node scripts/verify-cdp.js",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name shape-onboarding --branch main --commit-hash local-demo --commit-message \"Public shape onboarding demo\" --commit-dirty=true"
  }
}
